# 写作指南

以后发布文章只需要记住三件事：

1. 复制 `content/templates/post.md` 到 `content/drafts/`。
2. 修改文章资料和正文，草稿阶段保持 `draft: true`。
3. 准备发布时，将文件移动到 `content/posts/`，并将 `draft` 改为 `false`。
4. 使用 GitHub Desktop 提交并推送。

## 文件命名

使用：

```text
年-月-日-英文短标题.md
```

示例：

```text
2026-07-20-rainy-day.md
2026-08-01-anime-review.md
```

## 图片位置

```text
assets/posts/文章ID/图片.webp
```

正文引用：

```markdown
![图片说明](assets/posts/文章ID/图片.webp)
```

## 草稿

```yaml
draft: true
```

草稿不会发布。准备好后移动到 `content/posts/`，并改为：

```yaml
draft: false
```

详细字段说明见仓库根目录的 `README.md`。

## 每日学习笔记

1. 复制 `content/templates/learning-note.md` 到 `content/drafts/`。
2. 文件名改为 `年-月-日-英文短标题.md`。
3. 保留 `category: 学习笔记`，写完后移动到 `content/posts/`，并将 `draft: true` 改为 `draft: false`。
4. 用 GitHub Desktop 提交并推送。

发布后的笔记会显示在首页与“技术”“随笔”“ACG”等并列的“学习笔记”分类中。
